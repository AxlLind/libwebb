on: push
name: Lints and tests
jobs:
  lints-and-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check format
        run: make check-format
      - name: Lints
        run: make lint
      - name: Tests
        run: make run-tests
