#include "internal.h"
#include "libtest.h"
#include "tmpfile.h"
#include "webb/webb.h"

static TmpFile file;

TEST(test_http_conn_next) {
  // TODO: Add some tests?
  (void) file;
  ASSERT(1 == 1);
}

TEST_MAIN(test_http_conn_next)
