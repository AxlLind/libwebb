#include "internal.h"
#include "libtest.h"
#include "tmpfile.h"
#include "webb/webb.h"

static TmpFile TMPFILE;

TEST(test_http_conn_next) {
  // TODO: Add some tests?
  (void) TMPFILE;
  ASSERT(1 == 1);
}

TEST_MAIN(test_http_conn_next)
